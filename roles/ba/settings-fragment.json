{
  "_note": "BA/Define role: build a UI prototype + deploy a preview + inspect with Playwright. frontend-design is already baked in the base; listed again to be explicit. Vercel + Playwright are NOT baked in the base image: enable a preview-deploy MCP/skill when needed, and Playwright needs the :playwright image plus `claude mcp add playwright -- playwright-mcp --headless`.",
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": true
  },
  "permissions": {
    "allow": [
      "Bash(gh *)",
      "Bash(vercel *)"
    ]
  }
}
