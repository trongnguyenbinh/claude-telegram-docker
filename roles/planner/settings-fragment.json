{
  "_note": "Planning role: issue-tracker-heavy (create items, link sub-tasks, add to board, label). Bash(gh *) is already in the base; listed again to make intent clear. No extra plugins needed beyond the baked ones.",
  "enabledPlugins": {},
  "permissions": {
    "allow": [
      "Bash(gh *)"
    ]
  }
}
